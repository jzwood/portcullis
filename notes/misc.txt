

-- Notes:
-- no Var (variables): instead you have functions with zero parameters
-- no Extern (external): we're just not gonna let you call external functions :/
-- TODO steal from this: https://blog.sumtypeofway.com/posts/introduction-to-recursion-schemes.html


--pipeToExpr :: Map Name Address -> Pipe -> Expr
--pipeToExpr addressMap Pipe { funcName, inAddressNames, outAddressName } =
  --Val $ Tuple (strToExpr funcName) (Val $ Tuple addressExprs (strToExpr outAddressName))
    --where
      --strToExpr :: String -> Expr
      --strToExpr str = Val (List CharType (Val . Character <$> str))
      --addressExprs = Val $ List CharType (inAddressNames <&> \name -> Val $ Tuple (strToExpr name) (Val $ Number (fromIntegral (buffer (addressMap ! name)))))

